// Deferred page. The full conversational interface (wired to providerRouter)
// is planned for the next milestone. Component is kept so the API layer in
// services/api/ has a consumer to ship against.

export default function Chat() {
  return (
    <section>
      <h1>Chat</h1>
      <p>Deferred. The conversational interface lands in the next milestone.</p>
    </section>
  )
}
